/* Einfaches Hello-World-Beispiel in C */

#include <stdio.h> // Ein- und Ausgabe (Konsole, Datein etc.)

int main() {
    // Printf (f = format):
    // - %s: ersetzt das %s durch den gegebenen String (in diesem Fall "world")
    // - \n: Zeilenende (wird in Pythons print automatisch angefügt)
    printf("Hello, %s!\n", "world");
}